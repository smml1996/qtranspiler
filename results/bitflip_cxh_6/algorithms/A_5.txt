x2 := measure(q16); 
if c = 0:
	x2 := measure(q16); 
	if c = 0:
		x2 := measure(q16); 
		if c = 0:
			x2 := measure(q16); 
			if c = 0:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
		if c = 4:
			x2 := measure(q16); 
			if c = 0:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
	if c = 4:
		x2 := measure(q16); 
		if c = 0:
			x2 := measure(q16); 
			if c = 0:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
		if c = 4:
			x2 := measure(q16); 
			if c = 0:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
if c = 4:
	x2 := measure(q16); 
	if c = 0:
		x2 := measure(q16); 
		if c = 0:
			x2 := measure(q16); 
			if c = 0:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
		if c = 4:
			x2 := measure(q16); 
			if c = 0:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
	if c = 4:
		x2 := measure(q16); 
		if c = 0:
			x2 := measure(q16); 
			if c = 0:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
		if c = 4:
			x2 := measure(q16); 
			if c = 0:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q16); 
				if c = 0:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q16); 
					if c = 0:
						HALT
					if c = 4:
						HALT
