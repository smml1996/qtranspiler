H([q18]); 
x2 := measure(q18); 
if c = 0:
	H([q18]); 
	x2 := measure(q18); 
	if c = 0:
		H([q18]); 
		x2 := measure(q18); 
		if c = 0:
			HALT
		if c = 4:
			HALT
	if c = 4:
		H([q18]); 
		x2 := measure(q18); 
		if c = 0:
			HALT
		if c = 4:
			HALT
if c = 4:
	H([q18]); 
	x2 := measure(q18); 
	if c = 0:
		H([q18]); 
		x2 := measure(q18); 
		if c = 0:
			HALT
		if c = 4:
			HALT
	if c = 4:
		H([q18]); 
		x2 := measure(q18); 
		if c = 0:
			HALT
		if c = 4:
			HALT
