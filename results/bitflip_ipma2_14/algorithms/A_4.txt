X([q21]); 
CNOT([q21,q18]); CNOT([q21,q17]); 
x2 := measure(q18); 
if c = 0:
	x2 := measure(q18); 
	if c = 0:
		x2 := measure(q18); 
		if c = 0:
			x2 := measure(q18); 
			if c = 0:
				HALT
			if c = 4:
				HALT
		if c = 4:
			x2 := measure(q18); 
			if c = 0:
				HALT
			if c = 4:
				HALT
	if c = 4:
		x2 := measure(q18); 
		if c = 0:
			x2 := measure(q18); 
			if c = 0:
				HALT
			if c = 4:
				HALT
		if c = 4:
			X([q21]); 
			HALT
if c = 4:
	x2 := measure(q18); 
	if c = 0:
		x2 := measure(q18); 
		if c = 0:
			x2 := measure(q18); 
			if c = 0:
				HALT
			if c = 4:
				HALT
		if c = 4:
			X([q21]); 
			HALT
	if c = 4:
		X([q21]); 
		x2 := measure(q18); 
		if c = 0:
			HALT
		if c = 4:
			HALT
