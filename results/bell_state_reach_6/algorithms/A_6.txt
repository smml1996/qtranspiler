H([q0]); CNOT([q0,q1]); 
CNOT([q0,q1]); H([q0]); x0 := measure(q0); x1 := measure(q4); 
if c = 0:
	H([q0]); CNOT([q0,q1]); 
	HALT
if c = 1:
	H([q0]); CNOT([q0,q1]); 
	HALT
if c = 2:
	H([q0]); CNOT([q0,q1]); 
	HALT
if c = 3:
	H([q0]); CNOT([q0,q1]); 
	HALT
