CNOT([q11,q14]); CNOT([q11,q16]); 
x2 := measure(q14); 
if c = 0:
	x2 := measure(q14); 
	if c = 0:
		x2 := measure(q14); 
		if c = 0:
			X([q11]); 
			X([q11]); 
			x2 := measure(q14); 
			if c = 0:
				HALT
			if c = 4:
				HALT
		if c = 4:
			x2 := measure(q14); 
			if c = 0:
				X([q11]); 
				X([q11]); 
				HALT
			if c = 4:
				x2 := measure(q14); 
				if c = 0:
					x2 := measure(q14); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q11]); 
					HALT
	if c = 4:
		x2 := measure(q14); 
		if c = 0:
			x2 := measure(q14); 
			if c = 0:
				X([q11]); 
				X([q11]); 
				HALT
			if c = 4:
				x2 := measure(q14); 
				if c = 0:
					x2 := measure(q14); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q11]); 
					HALT
		if c = 4:
			x2 := measure(q14); 
			if c = 0:
				x2 := measure(q14); 
				if c = 0:
					x2 := measure(q14); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q11]); 
					HALT
			if c = 4:
				X([q11]); 
				x2 := measure(q14); 
				if c = 0:
					HALT
				if c = 4:
					HALT
if c = 4:
	x2 := measure(q14); 
	if c = 0:
		x2 := measure(q14); 
		if c = 0:
			x2 := measure(q14); 
			if c = 0:
				X([q11]); 
				X([q11]); 
				HALT
			if c = 4:
				x2 := measure(q14); 
				if c = 0:
					x2 := measure(q14); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q11]); 
					HALT
		if c = 4:
			x2 := measure(q14); 
			if c = 0:
				x2 := measure(q14); 
				if c = 0:
					x2 := measure(q14); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q11]); 
					HALT
			if c = 4:
				X([q11]); 
				x2 := measure(q14); 
				if c = 0:
					HALT
				if c = 4:
					HALT
	if c = 4:
		x2 := measure(q14); 
		if c = 0:
			x2 := measure(q14); 
			if c = 0:
				x2 := measure(q14); 
				if c = 0:
					x2 := measure(q14); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q11]); 
					HALT
			if c = 4:
				X([q11]); 
				x2 := measure(q14); 
				if c = 0:
					HALT
				if c = 4:
					HALT
		if c = 4:
			X([q11]); 
			X([q11]); 
			X([q11]); 
			HALT
