CNOT([q0,q1]); H([q0]); x0 := measure(q0); x1 := measure(q2); 
if c = 0:
	H([q0]); CNOT([q0,q1]); 
	x2 := measure(q2); 
	if c = 0:
		HALT
	if c = 4:
		HALT
if c = 1:
	H([q0]); CNOT([q0,q1]); 
	x2 := measure(q2); 
	if c = 1:
		HALT
	if c = 5:
		HALT
if c = 2:
	H([q0]); CNOT([q0,q1]); 
	x2 := measure(q2); 
	if c = 2:
		HALT
	if c = 6:
		HALT
if c = 3:
	H([q0]); CNOT([q0,q1]); 
	x2 := measure(q2); 
	if c = 3:
		HALT
	if c = 7:
		HALT
