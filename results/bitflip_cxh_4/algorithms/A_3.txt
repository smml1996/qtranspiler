H([q8]); 
x2 := measure(q8); 
if c = 0:
	H([q8]); 
	x2 := measure(q8); 
	if c = 0:
		H([q8]); 
		x2 := measure(q8); 
		if c = 0:
			HALT
		if c = 4:
			HALT
	if c = 4:
		H([q8]); 
		x2 := measure(q8); 
		if c = 0:
			HALT
		if c = 4:
			HALT
if c = 4:
	H([q8]); 
	x2 := measure(q8); 
	if c = 0:
		H([q8]); 
		x2 := measure(q8); 
		if c = 0:
			HALT
		if c = 4:
			HALT
	if c = 4:
		H([q8]); 
		x2 := measure(q8); 
		if c = 0:
			HALT
		if c = 4:
			HALT
