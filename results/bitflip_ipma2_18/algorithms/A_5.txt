CNOT([q3,q5]); CNOT([q3,q4]); 
x2 := measure(q5); 
if c = 0:
	x2 := measure(q5); 
	if c = 0:
		x2 := measure(q5); 
		if c = 0:
			x2 := measure(q5); 
			if c = 0:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
		if c = 4:
			x2 := measure(q5); 
			if c = 0:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q3]); 
					HALT
	if c = 4:
		x2 := measure(q5); 
		if c = 0:
			x2 := measure(q5); 
			if c = 0:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q3]); 
					HALT
		if c = 4:
			x2 := measure(q5); 
			if c = 0:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q3]); 
					HALT
			if c = 4:
				X([q3]); 
				x2 := measure(q5); 
				if c = 0:
					HALT
				if c = 4:
					HALT
if c = 4:
	x2 := measure(q5); 
	if c = 0:
		x2 := measure(q5); 
		if c = 0:
			x2 := measure(q5); 
			if c = 0:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
			if c = 4:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q3]); 
					HALT
		if c = 4:
			x2 := measure(q5); 
			if c = 0:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q3]); 
					HALT
			if c = 4:
				X([q3]); 
				x2 := measure(q5); 
				if c = 0:
					HALT
				if c = 4:
					HALT
	if c = 4:
		x2 := measure(q5); 
		if c = 0:
			x2 := measure(q5); 
			if c = 0:
				x2 := measure(q5); 
				if c = 0:
					x2 := measure(q5); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q3]); 
					HALT
			if c = 4:
				X([q3]); 
				x2 := measure(q5); 
				if c = 0:
					HALT
				if c = 4:
					HALT
		if c = 4:
			X([q3]); 
			x2 := measure(q5); 
			if c = 0:
				x2 := measure(q5); 
				if c = 0:
					HALT
				if c = 4:
					HALT
			if c = 4:
				x2 := measure(q5); 
				if c = 0:
					HALT
				if c = 4:
					HALT
