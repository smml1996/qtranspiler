{
	x3 := measure(q3); 
	if c = 0:
		H([q0]); CNOT([q0,q1]); 
		HALT
	if c = 8:
		CNOT([q0,q1]); 
		HALT

} ⊕_0.000752 {
	x3 := measure(q3); 
	if c = 0:
		H([q0]); CNOT([q0,q1]); 
		x3 := measure(q3); 
		if c = 8:
			HALT
		if c = 0:
			HALT
	if c = 8:
		x3 := measure(q3); 
		if c = 0:
			H([q0]); CNOT([q0,q1]); 
			HALT
		if c = 8:
			CNOT([q0,q1]); 
			HALT
}
