{
	x3 := measure(q3); 
	if c = 0:
		CNOT([q0,q1]); 
		H([q0]); CNOT([q0,q1]); 
		HALT
	if c = 8:
		H([q0]); CNOT([q0,q1]); 
		H([q0]); CNOT([q0,q1]); 
		HALT

} ⊕_0.000704 {
	x3 := measure(q3); 
	if c = 0:
		CNOT([q0,q1]); 
		H([q0]); CNOT([q0,q1]); 
		HALT
	if c = 8:
		x3 := measure(q3); 
		if c = 0:
			H([q0]); CNOT([q0,q1]); 
			HALT
		if c = 8:
			CNOT([q0,q1]); 
			HALT
}
