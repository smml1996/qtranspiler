CNOT([q2,q3]); CNOT([q2,q4]); 
x2 := measure(q3); 
if c = 0:
	x2 := measure(q3); 
	if c = 0:
		CNOT([q2,q3]); CNOT([q2,q4]); 
		x2 := measure(q3); 
		if c = 0:
			X([q2]); 
			X([q2]); 
			HALT
		if c = 4:
			x2 := measure(q3); 
			if c = 0:
				x2 := measure(q3); 
				if c = 0:
					HALT
				if c = 4:
					HALT
			if c = 4:
				X([q2]); 
				HALT
	if c = 4:
		x2 := measure(q3); 
		if c = 0:
			x2 := measure(q3); 
			if c = 0:
				X([q2]); 
				X([q2]); 
				HALT
			if c = 4:
				x2 := measure(q3); 
				if c = 0:
					x2 := measure(q3); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q2]); 
					HALT
		if c = 4:
			x2 := measure(q3); 
			if c = 0:
				x2 := measure(q3); 
				if c = 0:
					x2 := measure(q3); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q2]); 
					HALT
			if c = 4:
				X([q2]); 
				x2 := measure(q3); 
				if c = 0:
					HALT
				if c = 4:
					HALT
if c = 4:
	x2 := measure(q3); 
	if c = 0:
		x2 := measure(q3); 
		if c = 0:
			x2 := measure(q3); 
			if c = 0:
				X([q2]); 
				X([q2]); 
				HALT
			if c = 4:
				x2 := measure(q3); 
				if c = 0:
					x2 := measure(q3); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q2]); 
					HALT
		if c = 4:
			x2 := measure(q3); 
			if c = 0:
				x2 := measure(q3); 
				if c = 0:
					x2 := measure(q3); 
					if c = 0:
						HALT
					if c = 4:
						HALT
				if c = 4:
					X([q2]); 
					HALT
			if c = 4:
				X([q2]); 
				x2 := measure(q3); 
				if c = 0:
					HALT
				if c = 4:
					HALT
	if c = 4:
		CNOT([q2,q3]); CNOT([q2,q4]); 
		x2 := measure(q3); 
		if c = 0:
			X([q2]); 
			x2 := measure(q3); 
			if c = 0:
				HALT
			if c = 4:
				HALT
		if c = 4:
			x2 := measure(q3); 
			if c = 0:
				X([q2]); 
				HALT
			if c = 4:
				x2 := measure(q3); 
				if c = 0:
					HALT
				if c = 4:
					HALT
