CNOT([q0,q1]); H([q0]); x0 := measure(q0); x1 := measure(q1); 
if c = 2:
	x2 := measure(q2); 
	if c = 6:
		HALT
	if c = 2:
		HALT
if c = 1:
	H([q0]); CNOT([q0,q1]); 
	HALT
if c = 0:
	H([q0]); CNOT([q0,q1]); 
	HALT
