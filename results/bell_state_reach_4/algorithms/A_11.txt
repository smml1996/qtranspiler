{
	x2 := measure(q2); 
	if c = 4:
		x2 := measure(q2); 
		if c = 0:
			HALT
		if c = 4:
			HALT
	if c = 0:
		x2 := measure(q2); 
		if c = 4:
			HALT
		if c = 0:
			HALT

} ⊕_0.992079 {
	CNOT([q0,q1]); H([q0]); x0 := measure(q0); x1 := measure(q2); 
	if c = 1:
		H([q0]); CNOT([q0,q1]); 
		HALT
	if c = 2:
		H([q0]); CNOT([q0,q1]); 
		HALT
	if c = 0:
		H([q0]); CNOT([q0,q1]); 
		HALT
}
