CNOT([q8,q6]); CNOT([q8,q5]); 
x2 := measure(q6); 
if c = 0:
	x2 := measure(q6); 
	if c = 0:
		x2 := measure(q6); 
		if c = 0:
			HALT
		if c = 4:
			HALT
	if c = 4:
		x2 := measure(q6); 
		if c = 0:
			HALT
		if c = 4:
			HALT
if c = 4:
	x2 := measure(q6); 
	if c = 0:
		x2 := measure(q6); 
		if c = 0:
			HALT
		if c = 4:
			HALT
	if c = 4:
		X([q8]); 
		HALT
