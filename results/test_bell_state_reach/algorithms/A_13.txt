{
	x3 := measure(q3); 
	if c = 0:
		H([q0]); CNOT([q14,q9]); 
		HALT
	if c = 8:
		CNOT([q0,q1]); 
		HALT

} ⊕_0.000221 {
	x3 := measure(q3); 
	if c = 0:
		x3 := measure(q3); 
		if c = 0:
			H([q0]); CNOT([q14,q9]); 
			HALT
		if c = 8:
			CNOT([q0,q1]); 
			HALT
	if c = 8:
		CNOT([q0,q1]); 
		HALT
}
