{
	x3 := measure(q3); 
	if c = 0:
		H([q0]); CNOT([q10,q5]); 
		HALT
	if c = 8:
		CNOT([q0,q1]); 
		HALT

} ⊕_0.002649 {
	x3 := measure(q3); 
	if c = 0:
		x3 := measure(q3); 
		if c = 0:
			H([q0]); CNOT([q10,q5]); 
			HALT
		if c = 8:
			CNOT([q0,q1]); 
			HALT
	if c = 8:
		CNOT([q0,q1]); 
		HALT
}
