{
	x3 := measure(q3); 
	if c = 0:
		H([q0]); CNOT([q10,q7]); 
		HALT
	if c = 8:
		CNOT([q0,q1]); 
		HALT

} ⊕_0.008682 {
	x3 := measure(q3); 
	if c = 0:
		H([q0]); CNOT([q10,q7]); 
		x3 := measure(q3); 
		if c = 8:
			HALT
		if c = 0:
			HALT
	if c = 8:
		x3 := measure(q3); 
		if c = 0:
			H([q0]); CNOT([q10,q7]); 
			HALT
		if c = 8:
			CNOT([q0,q1]); 
			HALT
}
