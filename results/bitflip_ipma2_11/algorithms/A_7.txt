CNOT([q24,q23]); CNOT([q24,q21]); 
x2 := measure(q23); 
if c = 0:
	x2 := measure(q23); 
	if c = 0:
		X([q24]); 
		X([q24]); 
		HALT
	if c = 4:
		x2 := measure(q23); 
		if c = 0:
			x2 := measure(q23); 
			if c = 0:
				HALT
			if c = 4:
				HALT
		if c = 4:
			X([q24]); 
			HALT
if c = 4:
	x2 := measure(q23); 
	if c = 0:
		x2 := measure(q23); 
		if c = 0:
			x2 := measure(q23); 
			if c = 0:
				HALT
			if c = 4:
				HALT
		if c = 4:
			X([q24]); 
			HALT
	if c = 4:
		X([q24]); 
		x2 := measure(q23); 
		if c = 0:
			HALT
		if c = 4:
			HALT
