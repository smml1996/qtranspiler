{
	x2 := measure(q2); 
	if c = 0:
		H([q0]); CNOT([q0,q1]); 
		HALT
	if c = 4:
		CNOT([q0,q1]); 
		HALT

} ⊕_0.000034 {
	x2 := measure(q2); 
	if c = 0:
		H([q0]); CNOT([q0,q1]); 
		x2 := measure(q2); 
		if c = 4:
			HALT
		if c = 0:
			HALT
	if c = 4:
		x2 := measure(q2); 
		if c = 0:
			H([q0]); CNOT([q0,q1]); 
			HALT
		if c = 4:
			CNOT([q0,q1]); 
			HALT
}
