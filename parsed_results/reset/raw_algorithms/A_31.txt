{
	x0 := measure(q0); 
	if c = 1:
		x0 := measure(q0); 
		if c = 1:
			X([q0]); 
			HALT
		if c = 0:
			HALT
	if c = 0:
		HALT

} ⊕_0.507089 {
	x0 := measure(q0); 
	if c = 0:
		x0 := measure(q0); 
		if c = 1:
			X([q0]); 
			HALT
		if c = 0:
			HALT
	if c = 1:
		X([q0]); 
		HALT
}
